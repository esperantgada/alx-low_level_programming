#include "main.h"

/**
 *void more_numbers - print number from 0 to 14
 *
 *Return: no return value
*/

void more_numbers(void)
{
	char i, j;

	for (i = '0'; i <= '10'; i++)
	{
		for (j = '0'; j <= '14'; j++)
		{
			_putchar(j);

		}
		_putchar('\n');
	}
	x
}
